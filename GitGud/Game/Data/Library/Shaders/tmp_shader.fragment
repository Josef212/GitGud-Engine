#version 330 core
in vec3 outNormal;
in vec2 outUv; 
in vec3 outColor;
out vec4 FragColor;
void main()
{
	//FragColor = vec4(outColor, 1.0);
	//FragColor = vec4(0.7, 0.7, 0.7, 1.0); 
	FragColor = vec4(outNormal, 1.0); 
}
